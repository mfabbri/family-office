# Timeline Rules

Questa cartella contiene policy tecniche per ordinare e validare eventi `timeline-events/v1`.

`default-overlap-policy.json` non e' un rule pack fiscale, previdenziale o legale: definisce solo priorita' di ordinamento e controlli di conflitto tra eventi dichiarati dall'utente.
